(function(){//closure
	var app=angular.module('LibraryWebsite').config(function($translateProvider){
	});//angular is the library, store the name of the module and the [] stands for dependencies. 

	
	app.controller('MainController',function($translate,$scope, ProjectsTitles,$location, $anchorScroll){//defines a controller.

	});

})();